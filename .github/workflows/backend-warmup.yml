name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl backend
        run: curl https://print-quote-mvp.onrender.com/health